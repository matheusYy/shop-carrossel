import { datavalue } from "../assets/data.img";

function returnData() {
  let data: Array<any> = [];
  datavalue.items.map((a) => {
    data.entries();
  });
  return data;
}

export default returnData();
